public class OptionsParser {

    /*public static MoveDirection[] parse(String[] direction) throws IllegalArgumentException {
        MoveDirection[] moveDirections = new MoveDirection[direction.length];
        for(int i = 0; i < direction.length; i++) {
            switch(direction[i]){
                case "f":
                case "forward":
                    moveDirections[i] = MoveDirection.FORWARD;
                    break;
                case "r":
                case "right":
                    moveDirections[i] = MoveDirection.RIGHT;
                    break;
                case "l":
                case "left":
                    moveDirections[i] = MoveDirection.LEFT;
                    break;
                case "b":
                case "backward":
                    moveDirections[i] = MoveDirection.BACKWARD;
                    break;
                default:
                    throw new IllegalArgumentException(direction[i] + " is not legal move specification");
            }
        }
        return moveDirections;
    }*/
}
